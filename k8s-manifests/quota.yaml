apiVersion: v1
kind: ResourceQuota
metadata:
  name: bank-dev-quota
  namespace: bank-dev
spec:
  hard: 
    pods: '20'
    requests.cpu: '4'
    requests.memory: 4Gi
    limits.cpu: '8'
    limits.memory: 8Gi
    persistentvolumeclaims: '5'